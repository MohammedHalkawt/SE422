package Mar_11th;
public class Main {
    public static void main(String[] args) {
        //5 if we have two instances of algo1, and one thread uses a and the thread uses b, then no problem, itnrinsic locks mean that if an object is locked, only one thread can access it at the same time. if they two objects then no problem one thread can work on each one.
    }
}
